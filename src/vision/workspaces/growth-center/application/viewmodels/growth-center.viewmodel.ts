/**
 * -----------------------------------------------------------------------------
 * TeoCRM Vision UI
 * Growth Center™
 * -----------------------------------------------------------------------------
 * Application - ViewModel
 *
 * ViewModel consumed by the Executive Workspace UI.
 *
 * During Vision UI v0.2.0 it is intentionally aligned with the
 * domain contract. Future versions may adapt formatting,
 * localization and presentation-specific properties without
 * impacting the domain model.
 * -----------------------------------------------------------------------------
 */

import type { GrowthCenterWorkspace } from "../../domain";

/**
 * ViewModel consumed by the Growth Center UI.
 */
export type GrowthCenterViewModel = GrowthCenterWorkspace;